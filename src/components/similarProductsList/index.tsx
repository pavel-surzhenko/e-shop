import { Box } from "@mui/material";

export const SimilarProductList: React.FC<{category: string}> = (props) => {
    console.log(props);
    
    return (
        <Box>
            
        </Box>
    );
};